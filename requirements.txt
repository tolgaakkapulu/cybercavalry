Django>=4.2.20,<4.3
ldap3==2.9.1
django-environ==0.11.2
cryptography>=44.0.1
abuseipdb-wrapper==0.1.6
APScheduler==3.10.4
redis>=4.6
reportlab>=4.2
svglib>=1.5,<1.6
whitenoise>=6,<7

# ── HTTP server per OS (environment markers) ────────────────────────
# Unix: gunicorn with native TLS (fork-based, does not run on Windows).
# Windows: hypercorn -- ASGI server with native TLS on Windows (waitress
# would work for plain HTTP but has no TLS support, and our deployment
# expects self-signed HTTPS on 8443 without a reverse proxy).
gunicorn>=22,<23 ; sys_platform != "win32"
hypercorn>=0.17 ; sys_platform == "win32"