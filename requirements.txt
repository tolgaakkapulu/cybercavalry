Django>=4.2.20,<4.3
ldap3==2.9.1
django-environ==0.11.2
cryptography>=44.0.1
abuseipdb-wrapper==0.1.6
APScheduler==3.10.4
redis>=4.6
reportlab>=4.2
svglib>=1.5,<1.6
whitenoise>=6,<7

# ── WSGI runners (installed per OS via environment markers) ─────────
# gunicorn is Unix-only (needs fork()); waitress is the Windows equivalent.
# Both are listed in the deploy scripts too, but keeping them here makes
# a plain `pip install -r requirements.txt` on any host self-sufficient.
gunicorn>=22,<23 ; sys_platform != "win32"
waitress>=3     ; sys_platform == "win32"